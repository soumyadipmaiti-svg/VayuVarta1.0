# Backend dependencies for Vayu Varta
# Install: pip install -r requirements.txt
# ── Web Framework ───────────────────────────────────────────────────
fastapi==0.115.5
uvicorn==0.32.1
# ── Config & Validation ───────────────────────────────────────────
# Pydantic 2.12.5 is the first pydantic stack with Python 3.14 wheels.
# Render uses Python 3.14 by default, so we pin the matching core up front
# instead of letting pip try to build pydantic-core from source via Rust.
pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings==2.6.1
pydantic[email]==2.12.5
python-dotenv==1.0.1
typing-inspection==0.4.4
# ── Database ──────────────────────────────────────────────────────
supabase==2.10.0
# ── Authentication ────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
# ── HTTP Client ──────────────────────────────────────────────────
httpx==0.27.2
# ── AI / LLM ─────────────────────────────────────────────────────
google-generativeai==0.8.3
# ── Web Push Notifications (Zomato/Swiggy-style alerts) ───────────
pywebpush==2.0.0
py-vapid==1.9.1
apscheduler==3.10.4
python-multipart==0.0.12
